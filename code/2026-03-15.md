# 2026-03-15

Context: the current repo is `quantide-founder`. `journal` submodule is the working journal by person and AI together. `platfrom` is the collab platform for person and AI.

Task: let us develop platform. first let us prepare data.
1. Copy `journal/default/2026-03-15.md` to `platform/tests/fixtures/default` as input fixture.
2. Read `journal/product/thera/2026-03-15.md`, generate a output fixture to `platform/tests/fixtures/default` and write a developer guide at `platform/docs/dev/default`.
3. Summarize the developer guide for me to approve.
